<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.socialnetwork.dao.UserMapper">
  <resultMap id="UserResult" type="com.solvd.socialnetwork.models.User">
    <id property="id" column="id" />
    <result property="firstName" column="firstName" />
    <result property="lastName" column="lastName" />
    <result property="age" column="age" />
    <result property="email" column="email" />
    <result property="phoneNumber" column="phoneNum" />
  </resultMap>

  <select id="findById" resultMap="UserResult" parameterType="Long">
    SELECT * FROM users WHERE id = #{id}
  </select>

  <insert id="insert" parameterType="com.solvd.socialnetwork.models.User" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO users (first_name, last_name, email, password)
    VALUES (#{firstName}, #{lastName}, #{email}, #{password})
  </insert>

  <update id="update" parameterType="com.solvd.socialnetwork.models.User">
    UPDATE users
    SET first_name = #{firstName}, last_name = #{lastName}, email = #{email}
    WHERE id = #{id}
  </update>

  <delete id="delete" parameterType="long">
    DELETE FROM users WHERE id = #{id}
  </delete>
</mapper>
